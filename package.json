{
  "name": "canopy-reader",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "migrate:local": "./scripts/migrate.sh --local",
    "migrate:remote": "./scripts/migrate.sh --remote",
    "deploy:web": "turbo deploy --filter=@canopy/web"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "typescript": "^5.7.0"
  },
  "packageManager": "pnpm@9.15.4"
}
